/*
    free() function:
        We can use the free function to de allocte the memory.
        The memory allocated using calloc/malloc is not deallocated automatically.

    SYNTAX:
        free(ptr);
*/